public enum ItemType
{
    Empty,
    Multicon,
    Invi,
    Pendulum,
    Deform,
    Rever,
}

public enum SceneIndex
{
    Title,
    Game,
    Result
}

public enum VFXType{
    A,
    B,
}

public enum SFXType{
    C,
    D,
}

public enum SpriteType{
    Default,
    GetItem,
    Die,
}