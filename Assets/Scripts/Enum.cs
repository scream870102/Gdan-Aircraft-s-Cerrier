public enum ItemType
{
    Empty,
    Multicon,
    Invi,
    Pendulum,
    Deform,
    Rever,
}

public enum SceneIndex
{
    Title,
    Game,
    Result
}

public enum VFXType
{
    A,
    B,
}

[System.Serializable]
public enum SFXType
{
    Attack,
    Damage,
    ItemGet,
}

public enum SpriteType
{
    Default,
    GetItem,
    Die,
}